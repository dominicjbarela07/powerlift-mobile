# Session menu and dedicated muscle-group navigation — local release preparation

Owner authorized TestFlight 2.1 promotion of the existing DEV fixes, then explicitly
instructed: do not push in this run; stop immediately before pushing. No push or
publication is part of this preparation.

The Session actions sheet groups routine controls, positive completion and isolated
red cancellation. Existing handlers, destructive confirmation, self-coach gates,
SetLog-based removal protection, and X/backdrop/swipe dismissal remain intact.
Add and Swap share the strict dedicated muscle-group asset resolver. Programming
navigation uses that resolver too. Existing approved PNGs are unchanged. Serratus
and Neck lack dedicated artwork and show an explicit missing state, never an anatomy
avatar. Legitimate anatomical evidence remains on its existing components.

Canonical npm-start Metro lineage was certified. Native iPhone 17 simulator
screenshots verify the actual grouped menu and Add → Chest/Legs navigation. These
retain the completed two-pass DEV visual refinements documented in the earlier
Session actions and muscle-group navigation evidence folders. The original weak
areas (flat hierarchy, ambiguous destructive placement, navigation anatomy fallback)
are corrected. No Session mutation was performed during this walkthrough.

DEV TypeScript and the focused menu, artwork, composition, confirmation, dismissal,
resume, picker, anatomy-boundary and exact movement artwork tests passed. Final
release projection, native compatibility and export results are recorded separately
in the backend repository's matching validation folder.

Release scope: the Logger's canonical shipping snapshot starts at TestFlight r36
plus only the menu integration. Its former whole-file holdback is removed. Full
pending DEV equipment/history work is preserved byte-for-byte in the DEV working
file as an unstaged delta; it is not included in this commit or release. Other
pinned catalog/equipment holdbacks remain. No API, schema, native dependency,
canonical movement ID, account state or permission change is introduced.

State machine: menu visibility/dismissal and movement-picker navigation presentation.
Adjacent controls: Add/Remove, rest timer, Finish/Cancel, Programming taxonomy and
anatomical evidence. Focused interaction tests preserve the existing callbacks,
eligibility and safe dismissal; shared asset tests cover all eight regions and
22 governed muscle destinations. Production Mobile 2.0.2 remains untouched.
