# Movement artwork identity — DEV validation

Implementation: `ba7ec89e8e07ed2794f0183b4a4bc25ace82d1b7`.
The permanent contract and inventory are in `docs/CANONICAL_MOVEMENT_ARTWORK_LIFECYCLE.md`.

- 211/211 automatically discovered accepted mobile contracts pass.
- 60/60 cumulative release-critical areas pass, including the new lifecycle/consumer gate.
- TypeScript passes. Changed-file lint has zero errors and existing warnings; the shared normalization/resolution modules have zero warnings.
- Positive human approval, exact-image bytes, DEV-only photo boundaries, current 195 mappings, four retired mappings and independent legacy IDs remain governed.
- Normalization uses performed canonical evidence, snapshot taxonomy, exact IDs/keys and server-governed legacy references. All 18 consumers / 38 render sites use the common renderer, with identifiable DEV diagnostics.
- No app version, native dependency, API, auth or workspace ownership changes in DEV.

Backend/native evidence and the full pre→active→saved→completed identity matrix are
in the canonical backend repository at
`docs/validation/movement-art-lifecycle-2026-09-13/README.md` and `identity-audit.json`.
The before/after tool executes the previous DEV and TestFlight resolver sources
against identical saved API payloads. It reports 198 susceptible TestFlight
registrations, 195 current DEV registrations, 8/33 actual historical rows affected,
and zero unresolved governed rows after the fix. Thirty deterministic older legacy
mappings resolve without historical writes.

The dirty-tree Metro proof records canonical `npm start`; live runtime logs remain
outside version control. Native screenshots show the two requested movements in an
isolated copy, saved evidence, completion, fresh return, exact History and shared
performed review. Only the isolated QA Session/evidence was removed; all 17,932
original SetLogs remained byte-for-byte unchanged.

The previous gaze-audit test had an obsolete 198-current-assets assertion after
human approval retired four identities and added ID646. Its historical audit stays
198; the current-set assertion now explicitly honors those exact governed changes.
No asset approval/bytes changed and no test was quarantined.
