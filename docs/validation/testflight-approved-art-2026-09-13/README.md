# TestFlight approved movement art promotion — September 13, 2026

Owner authorization: promote all validated compatible DEV mobile work, OTA only.
Production mobile/backend/database/environment/metadata are outside this task.
Final publication and independently downloaded delivery receipts are recorded in
the backend DEV validation directory with this same date/name.

## Exact release inventory

The live pre-promotion channel serves iOS runtime **2.1.0**, existing native build
**28**, revision **15**: update `01a09bce-5f48-74fe-98d5-a49908acbda7`, group
`48995219-69ba-4e2b-9c0c-8cc53e3f47fa`. Its clean source projection is `301bbf1`,
descended from canonical DEV `8d08ad8`. DEV HEAD before this task was `648ef38`.
Both Git ancestry and the September 10–13 development ledgers were inspected.

| Scope | Classification | Evidence / action |
| --- | --- | --- |
| Approved free-weight family and final human decisions | Include after focused validation | `95f6a8d`: 195 positive mappings; previously present in source but behind `__DEV__`; enable TestFlight app/thumbnail bytes without modifying any image or decision. |
| Compact plan exact art; PRE/ACTIVE atmospheric hero plus anatomy; superset member coverage; focal/crop corrections | Include after focused validation | `648ef38`, dependent hero `bfc2334` and targeted-muscle cue `4503573`; same canonical consumers, positive receipts, independent machine/cable fallback. |
| Plate-load and selector-pin category close-ups | Include after focused validation | `ae6c61f`: two previously validated category cards, actual prior native save/reopen proof; enable TestFlight presentation without changing equipment writes. |
| Begin Session → readiness | Include, already shipped | `46e7f86`, revision 10. Current native Begin opens readiness. |
| Rest choice, +30, background persistence, Skip | Include, already shipped | `4fa9d26`, revision 11; permanent executable runtime tests remain required. |
| Stable and useful comparable exposure | Include, already shipped | `d1ef3de`, revision 12; snapshot/revalidation and exact-history contracts. |
| Canonical superset grouping and round progression | Include, already shipped | `a06e9bc`, revision 13; member identity and rest sequencing retained. |
| Exact Equipment subject and race protection | Include, already shipped | `3c739ba` / `a167646`, revision 14; same effective subject through picker/save/reconcile. |
| Completed/performed artwork identity and anatomy fallback | Include, already shipped | `ba7ec89` / `8d08ad8`, revision 15; no backend dependency added. |
| Programming Manager, canonical Logger V3, workspace/Performance/Ledger work | Include, already shipped | Existing revision-15 ancestry and protected behavior manifest; no separate old implementation or random cherry-picks. |
| Empty ACTIVE focus after browsing PRE then returning to plan | Include | Discovered in current native readiness smoke. Restore a valid first incomplete movement when the prior manual-selection flag has no surviving focused subject. Preserve valid manual focus. Actual effect regression test added. |
| Review web UI, candidate storage, masters, generation tools, native visual labs | Exclude — DEV only | Three declared `dev-mocks` routes omitted from projection; no customer review navigation; actual exported hashes exclude candidates/masters. |
| Accessory rebuild / native 3D experiment | Exclude — DEV only / unfinished experiment | Existing source-parity exclusions retained; no new native module. |
| DEV catalog/database edits (including new ankle-weight identity), QA fixtures and seed data | Exclude — backend off limits | No backend deployment or data promotion. A mobile receipt cannot invent catalog coverage; exact ID/key/taxonomy must match the API subject. |
| Native-incompatible completed mobile changes | None found | No dependency, plugin, entitlement, Info.plist, runtime or native project delta. Existing runtime compatibility guard remains mandatory. |

All meaningful source differences from revision 15 are the coherent approved-art
consumption change above, current runtime gating/export safeguards, focus repair,
their tests/docs, and declared app/eas/DEV-route projection differences. Existing
untracked artifacts and unrelated backend changes are preserved.

## Approval, rendering and delivery contract

`approvedArtRuntimeEnabled` enables DEV or the publisher's explicit TestFlight
build switch. Production defaults disabled. The registry keeps its build-time
condition inline for Metro asset pruning. Every exact consumer still requires
the governed normalized ID/key, taxonomy, positive human receipt and matching
candidate/app hash. Pending/rejected/missing artwork retains focused anatomy.

`assertHumanArtworkGate` verifies every mapped candidate and immutable image.
`assert-no-dev-artwork-export` additionally verifies actual exported bytes:
195 approved app images + 195 thumbnails, plus the two category images. No
unapproved, rejected or master bytes are allowed. The flag is not an approval.
No approval record, candidate version, canonical mapping or image was edited.

## Focused native evidence

Captured from canonical dirty-tree `npm start`, Expo Go, iPhone 17 / iOS 26.2:

| Movement | Native inspection | Release-mode executable coverage |
| --- | --- | --- |
| Dumbbell Curl 253 | Compact plan, expanded PRE, expanded ACTIVE; anatomy beside name, approved hero and comparable exposure | PRE/ACTIVE/remount and approved receipt |
| Incline Dumbbell Bench Press 33 | Compact plan and expanded PRE; readable title, chest cue and hero | PRE/ACTIVE/remount |
| One-Arm Dumbbell Row 154 | Compact plan and expanded PRE; back cue and hero | PRE/ACTIVE/remount |
| Bulgarian Split Squat 354 | Compact plan and expanded PRE; quadriceps cue and hero | PRE/ACTIVE/remount |
| Machine Shoulder Press 91 | Compact plan and expanded PRE; anatomy, no fake image or question mark | PRE/ACTIVE, covered/uncovered member switching |
| Cable Upright Row 120 | Expanded PRE; anatomy, no fake image or question mark | PRE/ACTIVE, independent coverage |

`plan.png`, `pre-*.png`, `active-253.png` and `readiness-on-begin.png` are actual
native captures. The previous first/second visual passes corrected title/photo
collision, a visible image edge and undersized thumbnail subjects; this pass
checked the resulting shared composition for the six representative movements.

Native limitation: the Mac locked after the Curl ACTIVE check. Automatic unlock
failed and owner unlock was requested. Remaining ACTIVE taps, a fresh native
recheck of the focus repair, and additional rest/superset interactions could not
be completed during this pass. These are not claimed as native passes. Real TSX
consumers run in both DEV and `__DEV__ = false` TestFlight mode across all 195
mappings, phases, member selections and fallback states; permanent lifecycle,
rest, equipment and preview contracts cover adjacent behavior. Per the existing
source-parity policy, unavailable physical tooling is explicitly recorded and
does not block an otherwise validated compatible OTA.

## Validation and blast radius

TypeScript passes. Targeted ESLint: zero errors, 135 existing warnings. The
accepted suite and all 60 release-critical areas are rerun on the exact release
projection by the publisher before export/upload; final counts are in the
backend publication receipt. New export tests require actual derivatives and
reject pending/rejected/master bytes. New focus tests execute the actual effect
for cleared/stale focus, valid manual selection, remount, completion and journal
hydration. The all-195 consumer suite explicitly runs both DEV and TestFlight.

State machines touched: approved-art runtime eligibility/presentation and
PRE-to-ACTIVE presentation focus. Equipment type cards change presentation only.
Adjacent systems: Set logging, rest handoff, supersets, Swap, exact Equipment,
History/exposure, recap, Coach read-only preview and route/journal restoration.
Those workflows remain in their existing owners and are covered by cumulative
contracts. No auth, onboarding, billing, entitlement, relationship, subject
authority, performed evidence or backend write implementation changed.

QA sessions are isolated local DEV records. Cleanup receipt proves the original
17,932 SetLogs unchanged. No real scheduled Session is begun or altered.

## OTA boundary

Release source descends from the current pushed canonical DEV commit, with only
the five declared projection paths differing. App revision becomes 16; app
version/runtime remain 2.1.0, existing native build remains 28. No EAS build,
binary upload, submission, build-number increment or native configuration change.
Only the TestFlight branch is published. Independent before/after downloads
compare Production 2.0.2 iOS and Android IDs, keys, sizes and SHA-256 hashes, plus
stable channel metadata. Final delivery receipt records the actual OTA ID and
asset verification. **PRODUCTION UNCHANGED.**
